services:
  thelounge:
    image: ghcr.io/thelounge/thelounge:latest
    container_name: thelounge
    ports:
      - "9000:9000"
    restart: always
    volumes:
      - ./thelounge:/var/opt/thelounge
    networks:
      - local_network

networks:
  local_network:
    external: true
